<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trabajo.juan.umovil">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:logo="@drawable/um48"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Principal"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Presentacion"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".configuracion.Configuracion"
            android:screenOrientation="portrait" />
        <activity
            android:name=".configuracion.Configuracion2"
            android:screenOrientation="portrait" />
        <activity
            android:name=".informacion.Articulo"
            android:screenOrientation="portrait" />
        <activity
            android:name=".informacion.Directorio"
            android:screenOrientation="portrait" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".informacion.Localizacion"
            android:label="@string/title_activity_localizacion"
            android:screenOrientation="portrait" />
        <activity
            android:name=".informacion.Oferta"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MisionVision"
            android:screenOrientation="portrait" />
        <activity
            android:name=".servicios.Estudiante"
            android:screenOrientation="portrait" />
        <activity
            android:name=".servicios.MateriaEstudiante"
            android:screenOrientation="portrait" />
        <activity
            android:name=".servicios.HojaDeVida"
            android:screenOrientation="portrait" />
        <activity
            android:name=".servicios.Materia"
            android:screenOrientation="portrait" />
        <activity
            android:name=".servicios.ProgramaMateria"
            android:screenOrientation="portrait" />
        <activity
            android:name=".servicios.MateriaCodigoEstudiante"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SplashScreen"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".informacion.CalcularNotas" />
        <activity android:name=".AcercaDe"></activity>
    </application>

</manifest>